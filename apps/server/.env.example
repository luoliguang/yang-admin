# 数据库连接（PostgreSQL）。复制为 .env 并填入真实密码。
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/yang_admin?schema=public"

# JWT
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="2h"
JWT_REFRESH_SECRET="change-me-refresh"
JWT_REFRESH_EXPIRES_IN="7d"

# 服务端口
PORT=3000
