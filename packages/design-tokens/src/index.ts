/**
 * @yang-admin/design-tokens — 跨栈设计令牌
 * 以 CSS 变量 + JSON token 形式定义主题，Vue / React 均可消费。
 * 完整令牌体系在 P1 阶段落地，此处先占位。
 */
export const tokensCssPath = new URL('./tokens.css', import.meta.url).pathname;
